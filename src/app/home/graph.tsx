'use client'

import { ReportDataRow } from "@/lib/services/database/interfaces"

export default function Graph({ reportData }: { reportData: ReportDataRow[] }) {
    return (
        <div></div>
    )
}
